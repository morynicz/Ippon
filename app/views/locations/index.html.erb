<div class="item_list">
  <table>
    <%= form_for(@location) do |l| %>
    <tr>
      <th><%= l.label :name %></th>
      <th></th>
    </tr>
    <tr>
      <td><%= l.text_field :name %></td>
      <td><div class="actions">
        <%= l.submit :create %>
      </div></tr>
      <% end %>
      <% @locations.each do |l| %>
      <tr>
        <td><%= l.name %></td>
        <td><div class="actions"><%= link_to :edit, edit_location_path(l) %></div></td>
      </tr>
      <% end %>
    </table>
  </div>
